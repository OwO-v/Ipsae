<Window
    x:Class="Ipsae.View.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Ipsae.View"
    xmlns:behavior="clr-namespace:Ipsae.Behavior"
    mc:Ignorable="d"
    Title="Ipsae IDS" Height="720" Width="1280" WindowStyle="None" FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="BgMain" Color="#0C0C0C"/>
        <SolidColorBrush x:Key="BgCard" Color="#111111"/>
        <SolidColorBrush x:Key="Border" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#DDDDDD"/>
        <SolidColorBrush x:Key="TextDim" Color="#444444"/>
        <SolidColorBrush x:Key="StatusGreen" Color="#44AA55"/>

        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#222222"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E74C3C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TileButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BgCard}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="Padding" Value="24,28"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="TileBd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TileBd" Property="Background" Value="#141414"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#222" BorderThickness="1">
        <Grid Background="{StaticResource BgMain}" x:Name="RootGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="{StaticResource BgMain}" behavior:WindowDragBehavior.IsEnabled="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="24,0,0,0">
                    <Image Source="/Resource/leaf.png" Width="20" Height="20" Margin="0,0,10,0"/>
                    <TextBlock Text="Ipsae IDS" FontSize="15" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                    <TextBlock Text="v1.0.0" FontSize="10" Foreground="#2A2A2A"
                               VerticalAlignment="Center" Margin="8,2,0,0"/>
                    <Border Width="1" Height="14" Background="{StaticResource Border}" Margin="16,0"/>
                    <Ellipse Width="6" Height="6" Fill="{StaticResource StatusGreen}" VerticalAlignment="Center"
                             Margin="0,0,6,0"/>
                    <TextBlock Text="ACTIVE" FontSize="11" Foreground="{StaticResource TextDim}"
                               VerticalAlignment="Center" x:Name="StatusLabel"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource WindowControlButton}"
                            x:Name="MinimizeButton" Click="MinimizeButton_Click">
                        <Image Source="/Resource/minimize.png" Width="12" Height="12"/>
                    </Button>
                    <Button Style="{StaticResource WindowControlButton}"
                            x:Name="MaximizeButton" Click="MaximizeButton_Click">
                        <Image Source="/Resource/maximize.png" Width="12" Height="12"/>
                    </Button>
                    <Button Style="{StaticResource CloseButtonStyle}"
                            x:Name="CloseButton" Click="CloseButton_Click">
                        <Image Source="/Resource/close.png" Width="12" Height="12"/>
                    </Button>
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" BorderBrush="{StaticResource Border}" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid x:Name="HomeGrid">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <UniformGrid Grid.Row="1" Grid.Column="1" Columns="3" Width="720">
                            <Button Style="{StaticResource TileButton}" x:Name="TileStatus"
                                    Click="TileStatus_Click" Margin="0,0,5,5">
                                <StackPanel>
                                    <TextBlock Text="서비스 상태" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                                    <TextBlock Text="동작 중" FontSize="12" Foreground="#555" Margin="0,0,0,18"/>
                                    <TextBlock Text="ACTIVE" FontSize="9" Foreground="#44AA55" FontWeight="SemiBold" x:Name="TileStatusSub"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource TileButton}" x:Name="TileEvents"
                                    Click="TileEvents_Click" Margin="5,0,5,5">
                                <StackPanel>
                                    <TextBlock Text="탐지 목록" FontSize="14" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                                    <TextBlock Text="1,284 검토" FontSize="12" Foreground="#555" Margin="0,0,0,18"
                                               x:Name="TileEventsValue"/>
                                    <TextBlock Text="0 THREATS" FontSize="9" Foreground="#88CCFF"
                                               FontWeight="SemiBold" x:Name="TileEventsSub"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource TileButton}" x:Name="TileWhitelist"
                                    Click="TileWhitelist_Click" Margin="5,0,0,5">
                                <StackPanel>
                                    <TextBlock Text="Whitelist IP" FontSize="14" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                                    <TextBlock Text="3개 등록" FontSize="12" Foreground="#555" Margin="0,0,0,18"
                                               x:Name="TileWhitelistValue"/>
                                    <TextBlock Text="3 ENTRIES" FontSize="9" Foreground="#AAAAAA"
                                               FontWeight="SemiBold" x:Name="TileWhitelistSub"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource TileButton}" x:Name="TileBlacklist"
                                    Click="TileBlacklist_Click" Margin="0,5,5,0">
                                <StackPanel>
                                    <TextBlock Text="Blacklist IP" FontSize="14" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                                    <TextBlock Text="1개 등록" FontSize="12" Foreground="#555" Margin="0,0,0,18"
                                               x:Name="TileBlacklistValue"/>
                                    <TextBlock Text="1 ENTRY" FontSize="9" Foreground="#CC6666"
                                               FontWeight="SemiBold" x:Name="TileBlacklistSub"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource TileButton}" x:Name="TileSettings"
                                    Click="TileSettings_Click" Margin="5,5,0,0">
                                <StackPanel>
                                    <TextBlock Text="시스템 설정" FontSize="14" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimary}" Margin="0,0,0,8"/>
                                    <TextBlock Text="설정 완료" FontSize="12" Foreground="#555" Margin="0,0,0,18"/>
                                    <TextBlock Text="CONFIGURED" FontSize="9" Foreground="#999999"
                                               FontWeight="SemiBold"/>
                                </StackPanel>
                            </Button>
                        </UniformGrid>
                    </Grid>

                    <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" Visibility="Collapsed"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
<UserControl x:Class="Ipsae.View.Pages.BlacklistIPPageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Stretch" Margin="20">
            <TextBlock FontSize="28" FontWeight="Bold" Text="Blacklist IP" Margin="0 0 0 20"/>
            <DataGrid AutoGenerateColumns="True" Height="300">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="IP Address" Binding="{Binding IpAddress}" Width="150"/>
                    <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="200"/>
                    <DataGridTextColumn Header="Blocked Date" Binding="{Binding BlockedDate}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
